from setuptools import setup, find_packages

setup(
    name="items_based_journal",
    version="1.0.0",
    description="Add Item selection to Journal Entry with auto account population",
    author="iffisays",
    author_email="iffisays@gmail.com",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
